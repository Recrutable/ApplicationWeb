{% extends 'base.html.twig' %}

{% block body %}
    <h3>Modification de l'offre : </h3>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="pull-right btn btn-success" type="submit" value="Valider" />
    {{ form_end(edit_form) }}

    <ul class="list-inline pull-right">
        <li>
            <a class="pull-right btn btn-default" href="{{ path('offres_index') }}">Retour à la liste des offres</a>
        </li>
    </ul>
{% endblock %}
