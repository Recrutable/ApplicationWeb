{% extends 'base.html.twig' %}

{% block body %}
    <h3 class="popover-title">Edition du profile</h3>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <div class="btn-toolbar">
        <input type="submit" class="btn btn-success pull-right" value="Valider" />
        <input type="button" class="btn btn-warning pull-right" value="Back" onClick="history.go(-1);return true;"></from>
    </div>
    {{ form_end(edit_form) }}
{% endblock %}
