{% extends 'base.html.twig' %}

{% block body %}
    <h1>Questionnaires</h1>
    <div class="container jumbotron">
        <table class="table">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ questionnaire.nom }}</td>
                </tr>
                <tr>
                    <th>Domaine</th>
                    <td>{{ questionnaire.domaine }}</td>
                </tr>
                <tr>
                    <th>Type</th>
                    <td>{{ questionnaire.type }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('questions_new', { 'id_questionnaire': questionnaire.id }) }}" class="btn btn-success"> Ajouter des questions</a>

        <a href="{{ path('Qcm_index') }}" class="btn btn-info">Retour à la liste</a>
        {{ form_start(delete_form) }}
            <input type="submit" value="Supprimer" class="btn btn-danger">
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
