{% extends 'base.html.twig' %}

{% block body %}
    <h3>Questionnaires</h3>
    <div class="container jumbotron">
        <table class="table">
            <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ questionnaire.nom }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ questionnaire.domaine }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ questionnaire.type }}</td>
            </tr>
            </tbody>
        </table>

        <ul class="list-inline pull-right">
            <li>
                {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class="btn btn-danger">
                {{ form_end(delete_form) }}
            </li>
            <li>
                <a href="{{ path('questions_index', { 'id_questionnaires': questionnaire.id }) }}" class="btn btn-info"> Liste des questions</a>
            </li>
            <li>
                <a href="{{ path('Qcm_index') }}" class="btn btn-default">Retour à la liste</a>
            </li>
        </ul>
    </div>
{% endblock %}
