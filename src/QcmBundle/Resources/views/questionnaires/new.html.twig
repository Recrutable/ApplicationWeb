{% extends 'base.html.twig' %}

{% block body %}
    <h1>Questionnaires creation</h1>
    <div class="container jumbotron">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('Qcm_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
{% endblock %}
