{% extends 'base.html.twig' %}

{% block body %}
    <h1>Questions creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}
    <a class="btn btn-default" href="{{ path('Qcm_show', {id: id_questionnaire } ) }}" >
        Retourner au questionnaire
    </a>

    <ul>
        <li>
            <a href="{{ path('questions_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
