{% extends 'base.html.twig' %}

{% block body %}
    <h3>Modification</h3>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input class="btn btn-success pull-right" type="submit" value="Modifier" />
    {{ form_end(edit_form) }}

    <ul class="list-inline pull-right">
        <li>
            <a  class="btn btn-default" href="{{ path('questions_index',{id_questionnaires: questionnaire.id }) }}">Retour a la liste des questions</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input class="btn btn-danger" type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
